# React Native's own dependency graph pins @types/react hard, and this package
# imports neither React nor React Native — the collectors are structural on
# purpose, so they can be tested without a native runtime. Resolving peers
# strictly would fail an install over a dependency nothing here uses.
legacy-peer-deps=true
